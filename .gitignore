# Build artifacts
dist/
build/
*.exe
*.msi
*.zip

# Installer output (but not source scripts)
installer/output/
installer/*.exe
installer/*.msi

# Python
__pycache__/
*.pyc
*.pyo
.env

# Database (never commit client data)
*.db
data/
Customer\ Llist/

# OS metadata
desktop.ini

# PyInstaller (keep .spec in repo, ignore output)
# ShreejiAdmin.spec <- DO NOT ignore this, it should stay in repo

admin_login.log